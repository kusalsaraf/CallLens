# CallLens — Build Log

| Phase | Status | Commit | Date | Summary |
|-------|--------|--------|------|---------|
| 0 | done | `582d4b6` | 2026-06-07 | Monorepo layout, Python/FastAPI skeleton, docker-compose, CI workflow |
| 1A | done | `94bdec1` | 2026-06-07 | Single-user JWT auth: User model, Argon2 hashing, signup/login/refresh/logout/me |
| 1B | done | `3466905` | 2026-06-07 | Next.js frontend: marketing site, auth pages, protected app shell, 16 tests |
| 2A | done | `da913e8` | 2026-06-07 | Async processing spine: Team/Agent/Call/Transcript/Segment models, Alembic migration, local storage (aiofiles, Range streaming), stub transcriber + null diarizer (no heavy deps), Celery+Redis pipeline, 7 call API endpoints (upload, list, detail, transcript, audio, SSE, delete), 22 tests |
| 2B | done | TBD | 2026-06-07 | Upload page (drag-and-drop, XHR progress, SSE stepper), calls list (table, filters, pagination), call detail (audio player + two-way transcript sync via blob URL + fetch SSE) |
| 3A | done | `83c0a23` | 2026-06-07 | Scoring models (Rubric/CallScore/ScoreEvidence), LLM provider abstraction (stub+langchain), evidence validator, sentiment agent, scoring service + pipeline extension, scoring API (GET scores, POST reprocess), 23 tests |
| 3B | done | `41d822a` | 2026-06-08 | Evidence-linked scorecard: EvidenceChip, DimensionScoreCard, ScorecardPanel, two-column call-detail layout, focusedSegmentId wiring (audio seek + transcript highlight), score bands constants, scoring/scored status integration |
| 4A | done | `3cf8c5b` | 2026-06-08 | Multi-agent LangGraph scoring graph: TimedTranscriptSegmentData, ConversationMetrics (compute_metrics), FullRubricDimensionData, four specialist agents (sentiment, script, compliance, objection) + talk-listen ratio, supervisor node (weighted scoring, 3 escalation rules, LLM narrative), StateGraph fan-out with Send API, run_scoring_graph public API, 33 tests |
| 4B | done | `9bcd23e` | 2026-06-08 | Pipeline wiring + full persistence: CallAnalysis/CoachingNote/AuditLog/CallAgentRun ORM models, Phase 4B Alembic migration, score_call rewritten to call run_scoring_graph with one-transaction persistence, band computed field on CallScoreOut, GET /analysis + GET /trace + POST /reprocess (idempotent), GET /agents + GET /agents/{id} + GET /agents/{id}/coaching, POST/GET/DELETE /coaching-notes, 13 new API tests, 114 total |
| 4C | done | `246fcb4` | 2026-06-08 | Full call-detail page: OverallScoreHero (SVG ring gauge + band color), EscalationBanner (--fail toned, escalation_reason), ScorecardPanel (all 6 dimensions, API band labels, compliance pass/fail badge), SummaryActionsCard (summary + clickable key moments + action items), ConversationDynamics (agent/customer split bar, interruptions, monologue, turns), AgentRunTrace (collapsible multi-agent flow: preprocess → specialists → supervisor with provider/score/confidence/evidence/timing), apiBandToScoreBand() mapping, analysis+trace TanStack Query, 25 new tests (76 frontend total) |
| 6A | done | `8fdfaff` | 2026-06-09 | Manager dashboard analytics API: AnalyticsFilters Depends, AT_RISK/QUALITY/FAIL thresholds, GET /analytics/overview (calls_total, calls_scored, avg_overall_score, compliance_pass_rate, flagged_count), GET /analytics/quality-trends?bucket=day\|week, GET /analytics/score-distribution (10-pt histogram + band counts), GET /analytics/compliance (pass_rate + weekly trend), GET /analytics/flagged (paginated at-risk, newest-first), GET /analytics/leaderboard (agents ranked by avg, is_at_risk), GET /agents/{id}/performance (trend + dimension breakdown + vs_team), GET /teams/{id}/analytics (bands + agent_comparison); analytics_dataset fixture moved to conftest; 33 new tests (147 total) |
| 6A† | done | `cad4b27` | 2026-06-09 | Pre-6B reconciliation: canonical band thresholds (quality≥80, at-risk 60-79, fail<60) unified in core/scoring.py (single source of truth); CallScoreOut.band and all analytics predicates/aggregates migrated to use it; flagged predicate tightened to score<80 (band≠quality); frontend scoreBand() comment added, apiBandToScoreBand() updated to pass-through canonical strings; all divergent values deleted; 149 backend tests + 76 frontend tests green |
| 6B | done | `294ac21` | 2026-06-09 | Overview analytics dashboard: /app/overview as new landing tab (redirect from /app); AnalyticsFilters typed fetchers for all 6A endpoints (apiGetOverview, apiGetQualityTrends, apiGetScoreDistribution, apiGetCompliance, apiGetFlagged, apiListTeams); FilterBar with date presets (7/30/90/All) + team selector + URL search params; KpiCards (calls, avg score band-colored, compliance, flagged); QualityTrendChart (Recharts area, day/week toggle, reference lines at 80 + 60); ScoreDistributionChart (bar histogram, bars tinted by canonical band color); ComplianceCard (pass-rate stat + sparkline); FlaggedCallsTable (paginated, links to /app/calls/[id], empty state "No flagged calls in this range — nice."); CardSkeleton loading states; GET /api/v1/teams/ endpoint added to backend; 26 new frontend tests (102 frontend total); 149 backend tests still green |
| 6C | done | TBD | 2026-06-09 | People analytics views: /app/agents (leaderboard, sortable by all columns, at-risk/on-track badges, honors FilterBar date+team, row click → agent detail); /app/agents/[id] (header with avg score band-colored + at-risk indicator, weekly quality trend with Recharts line + 80/60 reference lines, dimension breakdown horizontal bars tinted by band, agent-vs-team paired bar with delta, coaching panel with auto/manual note distinction, manual note compose + delete with confirm, 404 state); /app/teams (team card grid listing); /app/teams/[id] (header stats, stacked score-distribution bar with canonical band colors, agent comparison table linking to /app/agents/[id], 404 state); api/agents.ts + api/teams.ts typed fetchers (apiGetAgents, apiGetAgent, apiGetAgentPerformance, apiGetAgentCoaching, apiCreateCoachingNote, apiDeleteCoachingNote, apiGetTeamAnalytics); Agents + Teams added to sidebar nav; IA note: Teams nav → /app/teams card grid → /app/teams/[id] detail; 23 new frontend tests (125 frontend total); 149 backend tests still green |
| 7A | done | TBD | 2026-06-09 | Segment embeddings + semantic search API: embeddings/ module (Embedder protocol, StubEmbedder deterministic from text hash, LocalEmbedder sentence-transformers bge-small-en-v1.5, GeminiEmbedder via langchain), pgvector Vector(384) column on TranscriptSegment with HNSW cosine index, Alembic migration (pgvector extension + column + index), pipeline wires embedding after transcription (resilient — failure leaves null, doesn't block call), idempotent backfill CLI (python -m calllens.embeddings.backfill), search_service (embed query → cosine similarity → group by call with snippets + band + agent), GET /api/v1/search?q=...&limit=... (auth-guarded, filters: agent_id, team_id, date_from, date_to, status), EMBEDDING_PROVIDER/EMBEDDING_DIM/EMBEDDING_MODEL_NAME settings, optional [embeddings] dep group (sentence-transformers), 18 new tests (167 backend total) |
| 7B | done | TBD | 2026-06-09 | Semantic search UI: /app/search page with natural-language input (URL ?q= param for shareable searches), results grouped by call (agent, band-colored score, relative time, top matching snippets with query-term highlighting + mm:ss timestamps + similarity %), snippet links to /app/calls/[id]?segment={id} for deep-linking, card links to call detail, FilterBar (date presets + team), initial empty state with clickable example queries, loading skeletons, no-results and error states; call detail ?segment= deep-link (auto-sets focusedSegmentId + seeks AudioPlayer on load, guards unknown IDs); Search activated in sidebar nav; api/search.ts typed fetcher (apiSearch via TanStack Query, keyed on q+filters, no-fire on empty q); 14 new frontend tests (139 frontend total); 167 backend tests still green |
| 8A | done | TBD | 2026-06-09 | Customizable rubrics: DimensionKind StrEnum (sentiment_empathy, script_adherence, compliance, objection_handling, talk_listen, outcome, custom) with per-kind Pydantic config validation (compliance→required_phrases, script→checklist, custom→guidance; 422 on invalid); Rubric CRUD service + API (GET /rubrics, GET /rubrics/{id}, POST /rubrics (inactive by default), PUT /rubrics/{id}, POST /rubrics/{id}/activate (exactly one active), POST /rubrics/{id}/clone, DELETE /rubrics/{id} (409 for active or call-referenced)); audit logs for create/update/activate/delete; is_active column on Rubric + rubric_id FK on Call (Alembic migration); upload binds call to active rubric (fallback to seeded Support QA); score_call uses call's bound rubric; reprocess supports rebind_rubric flag; score_custom generic specialist for custom-kind dimensions; graph dispatch expanded beyond score+ratio to all non-bool kinds; supervisor weight normalization verified (divides by total weight, not 100); graceful degradation for unknown/misconfigured dimensions; seed marks Support QA as is_active=True; 26 new tests (193 backend total); 139 frontend tests still green |
| 8B | done | TBD | 2026-06-09 | Rubric builder UI: lib/api/rubrics.ts typed fetchers + mutations (apiListRubrics, apiGetRubric, apiCreateRubric, apiUpdateRubric, apiActivateRubric, apiCloneRubric, apiDeleteRubric) with DimensionKind type + per-kind config shapes; /app/rubrics list page (ACTIVE badge in --quality tone, per-row Edit/Clone/Activate/Delete actions, confirm dialogs for activate + delete, 409 delete shows server reason inline, empty/loading/error states); RubricBuilder shared form component (zod + react-hook-form: name/description + dimension editor with add/remove, kind select swapping kind-specific config inputs — compliance→phrase chip input, script_adherence→editable checklist, custom→guidance textarea, others→hint text, live normalized weight %, client validation mirroring backend: compliance≥1 phrase, script≥1 item, custom non-empty guidance, weights>0, ≥1 dimension; server 422 mapped to inline error); /app/rubrics/new (create mode) + /app/rubrics/[id]/edit (edit mode with prefilled data + Activate affordance); Rubrics nav activated in sidebar; scoring-impact note in builder; 21 new frontend tests (160 frontend total); 193 backend tests still green |
| 9A | done | TBD | 2026-06-09 | PII/PCI redaction: redaction/ module with Redactor protocol + RedactionResult/RedactedEntity types; RegexRedactor (default, zero deps) — email, phone, US SSN, IPv4, Luhn-validated payment card numbers with typed placeholders ([REDACTED_EMAIL] etc.), idempotent on already-redacted text, entity offsets reference original text; PresidioRedactor (optional [redaction] dep group: presidio-analyzer + spacy) — NER entities (PERSON, LOCATION, etc.) + structured patterns, lazy-loaded engine singleton; get_redactor() factory keyed on REDACTION_PROVIDER (regex default); config: REDACTION_ENABLED (default true), REDACTION_PROVIDER (regex), REDACT_BEFORE_SCORING (true — privacy by default); ORM: redacted_text on TranscriptSegment, entities_redacted (JSONB count per entity type) + redaction_provider on Transcript, Alembic migration; pipeline wires redaction after segment persistence and BEFORE embedding (embedding still uses raw text — future toggle noted); resilient: per-segment redaction error logs and falls back to raw text, never fails the pipeline; idempotent backfill CLI (python -m calllens.redaction.backfill); redact-before-scoring: when REDACT_BEFORE_SCORING=true scoring graph receives redacted_text (fallback to raw), evidence validates against the same text the agent saw, no raw PII sent to LLM; GET /calls/{id}/transcript returns text + redacted_text + entities_redacted + redaction_provider (backward compatible — null when no redaction); 23 new tests (216 backend total); 160 frontend tests still green |
| 9B | done | TBD | 2026-06-09 | Transcript redaction UI + redacted search snippets: search_service prefers redacted_text over raw text in snippet display (ranking unchanged — still uses raw-text embeddings); frontend SegmentOut + TranscriptOut types updated with redacted_text, entities_redacted, redaction_provider; TranscriptPanel privacy-first toggle (default=Redacted, optional Show original with inline notice), PII indicator badge with typed entity counts (e.g. "PII redacted: 3 email, 1 card") in --at-risk tone or "No PII detected" when provider ran but found nothing; toggle re-renders text only — play-position highlight, click-to-seek, evidence/key-moment focus+flash all preserved (segment-id-based); call detail page wires entitiesRedacted + redactionProvider props; evidence chips and key-moment quotes render API-stored (already redacted) text unchanged; search snippets come back redacted from backend, term highlighting works cleanly over placeholders; existing test fixtures updated with redacted_text: null; 11 new frontend tests + 1 new backend test (171 frontend total, 217 backend total) |
| 10A | done | TBD | 2026-06-09 | Topic/theme tracking: Topic (id, name, slug unique, keywords JSONB) and CallTopic (call_id, topic_id, relevance float) ORM models + Alembic migration; idempotent topic taxonomy seed (10 support/collections topics: billing_dispute, cancellation_churn_risk, technical_issue, refund_request, payment_arrangement, complaint_escalation, account_access, product_question, delivery_shipping, retention_save — each with keyword lists); topics/ module with TopicExtractor protocol, StubKeywordExtractor (default — deterministic keyword-hit scoring, normalised relevance, threshold-aware, no deps/network), LLMTopicExtractor (optional, falls back to stub), get_topic_extractor() factory keyed on TOPIC_EXTRACTOR setting; pipeline wires _extract_topics after scoring (uses redacted text when REDACT_BEFORE_SCORING=true, replaces prior topics on reprocess, resilient — failure logged, never fails pipeline); idempotent backfill CLI (python -m calllens.topics.backfill); AnalyticsFilters extended with optional topic_id — _apply_filters JOINs call_topics when set, all analytics endpoints gain topic scoping for free (overview/quality-trends/flagged/leaderboard verified); GET /api/v1/analytics/topics (per-topic call_count, avg_overall_score + band, flagged_rate, honours date/team/agent filters, ordered by call_count desc); GET /api/v1/topics (list taxonomy) + GET /api/v1/topics/{id} (with stats) — auth-guarded; GET /api/v1/calls extended with topic_id filter; GET /calls/{id}/analysis includes topics array (topic_id, name, slug, relevance — ordered by relevance desc); TOPIC_EXTRACTOR + TOPIC_RELEVANCE_THRESHOLD config in core/config.py + .env.example; 20 new tests (237 backend total); 171 frontend tests still green |
| 10B | done | TBD | 2026-06-09 | Topics analytics UI: lib/api/topics.ts typed fetchers (apiGetTopicAnalytics, apiListTopics, apiGetTopic) with TopicAnalyticsEntry/TopicOut/TopicDetailOut types; /app/topics page with shared FilterBar (date/team), sortable table (call_count, avg score band-colored, flagged_rate % in at-risk/fail tone, click columns to sort asc/desc), Recharts horizontal bar chart (call volume per topic, bars tinted by avg band color), each topic row links to /app/calls?topic_id={id}, empty/loading/error states; calls list extended with ?topic_id= URL param, active "Topic: <name>" filter chip with clear control, topic selector dropdown from apiListTopics, status filter + pagination work alongside topic filter; CallAnalysisOut extended with topics: CallTopicBrief[] (topic_id, name, slug, relevance), call detail renders topic chips below OverallScoreHero linking to /app/calls?topic_id={id} ordered by relevance, renders nothing when empty; ListCallsParams extended with topic_id; Topics nav activated in sidebar; existing test fixtures updated with topics: []; 14 new frontend tests (185 frontend total); 237 backend tests still green |
| 11A | done | TBD | 2026-06-09 | Managed transcription + S3 storage: AssemblyAITranscriber (transcription/assemblyai.py) — uploads audio, requests transcription with speaker_labels=true, polls for completion (timeout + error handling), parses utterances into TranscriptSegmentData segments + caches DiarizationTurn list; PassthroughDiarizer (transcription/passthrough_diarizer.py) — retrieves cached speaker turns from AssemblyAITranscriber, falls back to single-speaker NullDiarizer behavior; merge maps raw labels (A/B) to agent/customer via existing two-most-frequent heuristic; TRANSCRIBER_PROVIDER=assemblyai auto-selects passthrough diarizer in factory; S3Storage (storage/s3.py) fully implemented — save, open_stream (with Range header pass-through to S3 GetObject), file_size (HeadObject), delete, exists (HeadObject); S3-compatible via S3_ENDPOINT_URL (works with Cloudflare R2, Backblaze B2, MinIO, Supabase Storage); boto3 client lazy-cached singleton; all ops run in thread pool executor (async); get_storage() factory passes S3_BUCKET from settings; config: ASSEMBLYAI_API_KEY, S3_BUCKET, S3_REGION, S3_ACCESS_KEY_ID, S3_SECRET_ACCESS_KEY, S3_ENDPOINT_URL — all with safe empty defaults; selecting assemblyai/s3 without required settings raises clear ValueError; optional dep groups: s3 (boto3), transcription-assemblyai (httpx); dev deps: boto3 + moto[s3] for testing; 26 new tests (263 backend total): AssemblyAI mocked httpx (segment parsing, diarization caching, passthrough→agent/customer mapping, error/timeout, polling, missing key), S3 moto (save/exists/stream/range/size/delete), factories (assemblyai/s3 returns right impl, missing settings raises, defaults unchanged), audio Range over S3, regression (stub/local/null all unchanged); 185 frontend tests still green |
| 11B | done | TBD | 2026-06-09 | Demo data seeder: is_demo boolean column + migration on calls table; scenario library (seed/scenarios.py) with 16 two-speaker support/collections call scripts spanning all 10 seeded topics — quality spread designed for fail/at-risk/quality bands, compliance violations and clean compliant calls, 5 agents across 2 teams (Collections East, Support West), synthetic PII throughout (fake emails, phones, Luhn-valid test cards 4111…, SSN, IPv4); seed/tts.py synthesize_call with injectable tts_fn — edge-tts for agent/customer voices (distinct en-US voices), pydub concatenation, optional "demo" dep group (edge-tts + pydub); demo seeder CLI (python -m calllens.seed.demo --count N --reset --audio/--no-audio): default --no-audio path injects scripts directly as Transcript + segments then runs full post-transcription pipeline (redaction → embedding → scoring → topics), calls reach status=scored fully offline with stub providers; --audio path synthesizes via edge-tts, stores via configured StorageBackend, runs full pipeline; idempotent (second run = no-op without --reset); --reset deletes ONLY is_demo=true calls + stored files (user data untouched); logs summary (calls created, band spread, flagged count, topics covered); transcript-direct import CLI (python -m calllens.seed.import_transcripts <file> --text-col --speaker-col --id-col): CSV/JSON/JSONL support, flexible column mapping, groups rows by call_id, creates Call + Transcript + segments directly then runs scoring pipeline, skips+logs malformed rows without aborting, documented public dataset sources; optional "demo" dep group in pyproject.toml (edge-tts, pydub) + mypy overrides; 16 new tests (279 backend total): scenarios (count, fields, unique IDs, PII presence, multi-team), TTS (mock voices, distinct agent/customer voices), demo seeder (creates scored calls, score variation, topics assigned, PII redacted, idempotent, reset scoped to demo only), transcript importer (CSV, JSON, malformed row skip); 185 frontend tests still green |
| 12B-prep | done | TBD | 2026-06-09 | Render blueprint + combined run mode (NO product changes): honcho added to base deps; backend/Procfile defines two processes (web: uvicorn with WEB_CONCURRENCY, worker: celery -A calllens.tasks.celery_app:celery_app with CELERY_CONCURRENCY) — honcho start runs both in a single service for free-tier hosting; Dockerfile updated to COPY Procfile and document combined/API-only/worker-only/migrate/seed run modes; render.yaml at repo root — web service from backend/Dockerfile (dockerContext: backend, plan: free, healthCheckPath: /health, preDeployCommand: alembic upgrade head, startCommand: honcho start), env vars with production defaults (APP_ENV=production, DB_USE_PGBOUNCER=true, SEED_DEMO_ON_START=true, WEB_CONCURRENCY=1, CELERY_CONCURRENCY=1, stub/regex providers), secrets with sync:false (JWT_SECRET generateValue:true, DATABASE_URL, REDIS_URL, API keys, S3 vars), commented-out dedicated worker service block (plan: starter, one-edit switch from combined to split); docs/DEPLOY.md with architecture diagram, run mode table, provider env-var matrix (free 512 MB: stub/regex/API-offloaded only vs paid: local embeddings/presidio), Render Blueprint deploy steps, external service recommendations (Supabase free, Upstash free, R2 free); 295 backend tests + 185 frontend tests still green |
| 12B-docker | done | TBD | 2026-06-09 | Full-stack docker compose (NO product changes): backend Dockerfile adds libmagic1 system dep (python-magic MIME sniffing); frontend multi-stage Dockerfile (node:20-slim deps→builder→runtime, standalone output, non-root calllens user, curl healthcheck, BACKEND_URL as build ARG + runtime ENV); frontend .dockerignore; next.config.ts adds output: "standalone"; docker-compose.yml replaces infra-only file with full stack — postgres (pgvector/pgvector:pg16, healthcheck, pgdata volume, port 5432), redis (redis:7-alpine, healthcheck, port 6379), migrate (alembic upgrade head, runs once, restart: no, depends_on postgres healthy), api (uvicorn, depends_on postgres+redis healthy + migrate completed, storage volume at /data/storage, healthcheck /health, port 8000), worker (celery -A calllens.tasks.celery_app:celery_app, shares storage volume with api, depends_on postgres+redis+migrate), frontend (standalone Next.js, BACKEND_URL=http://api:8000, depends_on api healthy, port 3000); named volumes pgdata + storage; root .env.example with all compose vars (APP_ENV=development, DATABASE_URL pointing at postgres service, REDIS_URL at redis service, JWT_SECRET dev placeholder, CORS_ORIGINS as JSON array, LOCAL_STORAGE_DIR=/data/storage, all stub/regex providers, SEED_DEMO_ON_START=true, BACKEND_URL=http://api:8000, commented provider upgrade guide); .env.example files updated to use JSON array syntax for list fields (pydantic-settings 2.14+ compat); backend .dockerignore no longer excludes uv.lock (needed for reproducible builds); docs/DEPLOY.md updated with quick-start (cp .env.example .env && docker compose up --build), infra-only mode, reset instructions, service table, shared volume note; both images verified: docker compose config valid, docker compose build api + frontend succeed; 295 backend tests + 185 frontend tests still green |
| export-audio | done | TBD | 2026-06-10 | Audio export CLI: python -m calllens.seed.export_audio [--count N] [--out DIR] [--scenario SLUG] — synthesizes scenario scripts to MP3 files on disk via synthesize_call (edge-tts + pydub), writes data/audio/<idx>_<slug>.mp3, creates output dir if missing, logs each file + total; injectable synthesize_fn for testing (bypasses pydub/edge-tts entirely); data/ added to .gitignore; 5 new tests (300 backend total): exports all scenarios, --count limits, --scenario single match, unknown scenario raises ValueError, creates nested output dir; 185 frontend tests still green |
| 12A | done | TBD | 2026-06-09 | Production deploy artifacts + hardening (NO new features): multi-stage Dockerfile (python:3.12-slim builder w/ uv sync --no-dev, runtime stage with non-root calllens user, PYTHONPATH=/app/src, CMD uvicorn with WEB_CONCURRENCY workers default 2); run modes documented: API (default CMD), Worker (celery override), Migrate (alembic upgrade head), Seed (python -m calllens.seed.demo); .dockerignore (excludes .venv, tests, __pycache__, .env, docs); production config hardening: model_validator rejects insecure defaults for JWT_SECRET + DATABASE_URL when APP_ENV=production (clear ValueError at startup — fail loud), docs_enabled property (disabled in prod by default, ENABLE_API_DOCS override); DB_USE_PGBOUNCER flag — disables asyncpg statement_cache_size + prepared_statement_cache_size for transaction-mode pooler compatibility (Supabase port 6543, PgBouncer); session.py wires connect_args conditionally; CORS from env var (no wildcard in prod); cookie Secure=true already wired on app_env==production; SecurityHeadersMiddleware (X-Content-Type-Options: nosniff, X-Frame-Options: DENY, Referrer-Policy: strict-origin-when-cross-origin, HSTS in production only); AuthRateLimitMiddleware — in-memory sliding-window per-IP rate limit on login/signup/refresh (configurable AUTH_RATE_LIMIT_PER_MINUTE, default 20), returns 429 on excess, clear_rate_limit_store() for tests; SEED_DEMO_ON_START flag in lifespan (runs demo seeder --no-audio after migrations if no demo data exists, failure logged and non-blocking); API docs gated: /docs /redoc disabled when docs_enabled=False (production default), overridable via ENABLE_API_DOCS=true; frontend .env.production.example documenting BACKEND_URL (Next.js rewrite keeps httpOnly cookie first-party); .env.example updated with all prod vars (DB_USE_PGBOUNCER, SEED_DEMO_ON_START, AUTH_RATE_LIMIT_PER_MINUTE, ENABLE_API_DOCS, APP_ENV notes); rate limit store cleared in conftest client fixture; 16 new tests (295 backend total): prod config validation (missing JWT_SECRET raises, missing DATABASE_URL raises, valid secrets succeed, cookie secure flag, dev defaults load, docs default dev/prod, docs force-enable, pgbouncer default), security headers (present on health, no HSTS in dev), docs gating (available in dev, 404 when off), auth rate limit (429 after limit, non-auth unaffected, normal login OK); 185 frontend tests still green |
